# extracted/ — disc-derived files (never committed)

`make extract` populates this directory with PRGs pulled from the disc
images in `ISOs/`, verified against the sha256 values recorded in
`config/targets/*.yaml`. Everything here except this README is gitignored:
these are Sega-derived bytes and must never enter the repository.
